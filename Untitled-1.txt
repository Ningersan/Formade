        case 'SAVE_QUESTIONNAIRE': {
            const editing = deepCopy(state.editing)
            const { list } = state
            const { questionnaireId, title, description, status, deadline, questions } = editing
            list[questionnaireId] = { title, description, status, deadline, questions }
            localStorage.list = JSON.stringify(list)
            console.log('saving')
            return { ...state, list }
        }

        case 'SAVE_QUESTIONNAIRE': {
            const list = deepCopy(state.list)
            const { editing } = state.editing
            const { questionnaireId } = editing
            const data = deepCopy(list[questionnaireId])
            list[questionnaireId] = { ...editing, data }
            localStorage.list = JSON.stringify(list)
            console.log('saving')
            return { ...state, list }
        }

        case 'FILL_QUESTIONNAIRE': {
            const { list } = state
            const editing = deepCopy(state.editing)
            editing.data = []
            list[editing.questionnaireId] = { ...editing }
            return { ...state, list, editing }
        }

    ::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background-color: rgba(0,0,0,.74);
    }

        const prevElement = dragElement.previousElementSibling
        const nextElement = dragElement.nextElementSibling
        const prevPrevElement = prevElement.previousElementSibling
        const nextNextElement = nextElement.nextElementSibling

        const { isFilled } = this.state
        isFilled[index] = false
        this.setState({
            isFilled,
        })